##basic_dockerfile → ~1.2 GB (what most freshers do)
FROM amazonlinux:latest
RUN yum update -y && yum install -y python3 python3-pip && pip3 install flask
COPY app.py /opt/app.py
CMD ["flask", "run", "--host=0.0.0.0", "--port=8080"]
